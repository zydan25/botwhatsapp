const state={install:null};
const $=(id)=>document.getElementById(id);
async function api(url,options={}){const r=await fetch(url,{credentials:'same-origin',...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});const d=await r.json().catch(()=>({}));if(r.status===401||r.status===403){location.href='/login';throw new Error('انتهت جلسة الدخول');}if(!r.ok||d.success===false)throw new Error(d.error||'حدث خطأ');return d.data;}
function esc(v){const d=document.createElement('div');d.textContent=String(v??'');return d.innerHTML;}
const labels={connected:'متصل',qr:'بانتظار QR',initializing:'جاري التشغيل',authenticated:'تمت المصادقة',disconnected:'متوقف',idle:'متوقف',error:'خطأ'};
function render(data){state.data=data;const s=data.session;$('sessionTitle').textContent=s.name;$('sessionDescription').textContent=s.description||'بدون وصف';$('clientStatus').textContent=labels[s.status]||s.status;$('clientStatus').className=`status-pill status-${esc(s.status)}`;$('qrContainer').innerHTML=s.qrDataUrl?`<img class="qr-image" src="${s.qrDataUrl}" alt="QR WhatsApp">`:`<div class="empty-box">${s.status==='connected'?'الجلسة متصلة بالفعل.':'اضغط تشغيل لإنشاء QR.'}</div>`;$('apiDetails').innerHTML=[['API Base URL',data.api.baseUrl],['Webhook الرسائل',data.api.webhookUrl],['Webhook الحالة',data.api.statusUrl],['Webhook QR',data.api.qrUrl],['Signature Header',data.api.signatureHeader],['Webhook Secret',s.webhookSecret]].map(([k,v])=>`<div class="detail-card"><span>${esc(k)}</span><div class="copy-row"><code>${esc(v)}</code><button class="btn small ghost" data-copy="${esc(v)}">نسخ</button></div></div>`).join('');$('accountDetails').innerHTML=`<div class="detail-card"><span>اسم العميل</span><strong>${esc(data.client.displayName||'—')}</strong></div><div class="detail-card"><span>اسم المستخدم</span><strong>${esc(data.client.username)}</strong></div><div class="detail-card"><span>العزل</span><strong>الجلسة المرتبطة فقط</strong></div>`;$('webhookExample').textContent=JSON.stringify(data.webhookExample,null,2);$('sessionJson').textContent=JSON.stringify({session:s,api:data.api,authentication:data.authentication,docs:data.docs},null,2);$('sessionDetails').innerHTML=[['الحالة',labels[s.status]||s.status],['رقم الحساب',s.info?.wid?.user||'—'],['المنصة',s.info?.platform||'—'],['بدأت',s.startedAt||'—'],['آخر تحديث',s.updatedAt||'—'],['الوارد',s.stats?.incomingCount??0],['الصادر',s.stats?.outgoingCount??0],['آخر نشاط',s.stats?.lastMessageAt||'—'],['API',s.apiBaseUrl||'—']].map(([k,v])=>`<div class="detail-card"><span>${esc(k)}</span><strong>${esc(v)}</strong></div>`).join('');renderMessages(s.messages||[]);renderEvents(s);}
function renderMessages(list){$('messagesTable').innerHTML=(list||[]).map(m=>`<tr><td>${esc(m.timestamp)}</td><td>${esc(m.direction)}</td><td>${esc(m.from||m.to||'—')}</td><td>${esc((m.body||'').slice(0,180))}${m.hasMedia?' · مرفق لم يتم تنزيله':''}</td></tr>`).join('')||'<tr><td colspan="4" class="empty-cell">لا توجد رسائل</td></tr>';}
function renderEvents(s){const all=[...(s.notifications||[]).map(n=>({t:n.timestamp,l:n.level,x:n.text})),...(s.errors||[]).map(e=>({t:e.timestamp,l:e.type,x:e.message}))].sort((a,b)=>String(b.t).localeCompare(String(a.t))).slice(0,30);$('eventsList').innerHTML=all.map(i=>`<div class="event"><span>${esc(i.t)}</span><strong>${esc(i.l)}</strong><p>${esc(i.x)}</p></div>`).join('')||'<div class="empty-box">لا توجد أحداث</div>';}
async function load(){render(await api('/api/client/me'));}
async function action(endpoint,message=''){if(message&&!confirm(message))return;document.body.classList.add('busy');try{await api(endpoint,{method:'POST',body:'{}'});await load();}catch(e){alert(e.message);}finally{document.body.classList.remove('busy');}}
$('startBtn').onclick=()=>action('/api/client/session/start');$('stopBtn').onclick=()=>action('/api/client/session/stop');$('restartBtn').onclick=()=>action('/api/client/session/restart','إعادة التشغيل تحافظ على المصادقة الحالية. متابعة؟');$('reauthBtn').onclick=()=>action('/api/client/session/reauthenticate','سيتم حذف بيانات اعتماد WhatsApp المحلية ثم إنشاء اتصال جديد بنفس API والوصف. متابعة؟');$('regenerateQr').onclick=()=>action('/api/client/session/qr/regenerate','سيتم إعادة تشغيل الاتصال لتوليد QR جديد. متابعة؟');$('refresh').onclick=()=>load().catch(e=>alert(e.message));
$('sendBtn').onclick=async()=>{const phone=$('sendPhone').value.trim();const message=$('sendMessage').value;if(!phone){alert('رقم الهاتف مطلوب');return;}try{const form=new FormData();form.append('phoneNumber',phone);form.append('message',message);const r=await fetch('/api/client/send',{method:'POST',credentials:'same-origin',body:form});const d=await r.json().catch(()=>({}));if(r.status===401||r.status===403){location.href='/login';return;}if(!r.ok||d.success===false)throw new Error(d.error||'فشل الإرسال');$('sendMessage').value='';await load();alert('تم إرسال الرسالة');}catch(e){alert(e.message);}};
$('changePassword').onclick=async()=>{const currentPassword=$('currentPassword').value;const newPassword=$('newPassword').value;if(newPassword.length<10){alert('كلمة المرور الجديدة يجب ألا تقل عن 10 أحرف');return;}try{await api('/api/client/password',{method:'POST',body:JSON.stringify({currentPassword,newPassword})});alert('تم تغيير كلمة المرور. سجّل الدخول مرة أخرى.');location.href='/login';}catch(e){alert(e.message);}};
$('clientApp').addEventListener('click',async(e)=>{const b=e.target.closest('[data-copy]');if(!b)return;try{await navigator.clipboard.writeText(b.dataset.copy);b.textContent='تم النسخ';setTimeout(()=>b.textContent='نسخ',1200);}catch{alert(b.dataset.copy);}});
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();state.install=e;$('installBtn').classList.remove('hidden');});$('installBtn').onclick=async()=>{if(!state.install)return;state.install.prompt();await state.install.userChoice;state.install=null;$('installBtn').classList.add('hidden');};
window.addEventListener('DOMContentLoaded',()=>{load().catch(e=>alert(e.message));navigator.serviceWorker?.register('/sw.js').catch(()=>{});if(typeof io==='function'){const socket=io({withCredentials:true});socket.on('connect',()=>{if(state.data?.session?.name)socket.emit('join-session',state.data.session.name);});socket.on('session:update',()=>load().catch(()=>{}));socket.on('session:notification',()=>load().catch(()=>{}));}setInterval(()=>load().catch(()=>{}),6000);});
