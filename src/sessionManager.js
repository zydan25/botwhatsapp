const path = require('path');
const fs = require('fs-extra');
const crypto = require('crypto');
const axios = require('axios');
const QRCode = require('qrcode');
const { Client, LocalAuth, MessageMedia } = require('whatsapp-web.js');
const { normalizePhoneToJid, nowIso, validateApiBaseUrl } = require('./utils');
const { resolveBrowserExecutable } = require('./browser');

class WhatsAppSession {
  constructor({ name, rootDir, store, apiBaseUrl = null, backupPhone = null, description = '', webhookSecret = null, chromePath = null, io = null }) {
    this.name = name; this.rootDir = rootDir; this.store = store; this.io = io; this.apiBaseUrl = validateApiBaseUrl(apiBaseUrl); this.backupPhone = backupPhone || null; this.description = String(description || '').slice(0, 1000); this.webhookSecret = webhookSecret || crypto.randomBytes(32).toString('hex'); this.chromePath = chromePath || process.env.CHROME_PATH || null; this.client = null; this.status = 'idle'; this.qr = null; this.info = null; this.lastError = null; this.startedAt = null; this.updatedAt = nowIso(); this.destroying = false; this.bootPromise = null; this.stopPromise = null; this.lastEvent = null; this.stats = { incomingCount: 0, outgoingCount: 0, lastMessageAt: null };
  }
  emit(event,payload){if(this.io)this.io.emit(event,payload)} roomEmit(payload){if(this.io)this.io.to(`session:${this.name}`).emit('session:update',payload);this.emit(`session:update:${this.name}`,payload)}
  notification(text,level='info',extra={}){const note={id:`${Date.now()}_${Math.random().toString(16).slice(2,8)}`,session:this.name,level,text,extra,timestamp:nowIso()};this.store.appendNotification(this.name,note).catch(()=>{});if(this.io)this.io.to(`session:${this.name}`).emit('session:notification',note);this.emit(`session:notification:${this.name}`,note);return note}
  async loadMeta(){const meta=await this.store.getSessionMeta(this.name);if(!meta)return;this.apiBaseUrl=validateApiBaseUrl(meta.apiBaseUrl??this.apiBaseUrl);this.backupPhone=meta.backupPhone??this.backupPhone;this.description=String(meta.description??this.description).slice(0,1000);this.webhookSecret=meta.webhookSecret||this.webhookSecret;this.status=meta.status||this.status;this.startedAt=meta.startedAt||this.startedAt;this.updatedAt=meta.updatedAt||this.updatedAt;this.lastError=meta.lastError||this.lastError;this.lastEvent=meta.lastEvent||this.lastEvent;this.stats=meta.stats||this.stats}
  serialize(){return{name:this.name,apiBaseUrl:this.apiBaseUrl,backupPhone:this.backupPhone,description:this.description,status:this.status,qrAvailable:!!this.qr,info:this.info,lastError:this.lastError,startedAt:this.startedAt,updatedAt:this.updatedAt,lastEvent:this.lastEvent,stats:this.stats}}
  async persistMeta(extra={}){this.updatedAt=nowIso();await this.store.setSessionMeta(this.name,{...this.serialize(),webhookSecret:this.webhookSecret,...extra,updatedAt:this.updatedAt});this.roomEmit({...this.serialize(),webhookSecret:this.webhookSecret,...extra,updatedAt:this.updatedAt})}
  async buildClient(){const executablePath=await resolveBrowserExecutable(this.chromePath);const args=['--no-sandbox','--disable-setuid-sandbox','--disable-dev-shm-usage','--disable-gpu','--disable-extensions','--disable-background-networking','--disable-default-apps','--disable-background-timer-throttling','--disable-renderer-backgrounding','--disable-features=TranslateUI'];return new Client({authStrategy:new LocalAuth({clientId:this.name,dataPath:path.join(this.rootDir,'sessions')}),fetchMessages:false,puppeteer:{headless:true,executablePath,timeout:600000,args}})}
  async start(){if(this.client&&!this.destroying)return this.serialize();if(this.bootPromise)return this.bootPromise;this.bootPromise=this._startInternal().finally(()=>{this.bootPromise=null});return this.bootPromise}
  async _startInternal(){this.destroying=false;this.status='initializing';this.qr=null;this.info=null;this.lastError=null;this.lastEvent={type:'start',at:nowIso()};this.startedAt=this.startedAt||nowIso();await this.persistMeta();this.notification('تم بدء تشغيل الجلسة','info');let client;try{client=await this.buildClient()}catch(error){this.status='error';this.lastError=error.message;this.lastEvent={type:'browser_error',at:nowIso()};await this.persistMeta({lastEvent:this.lastEvent});await this.recordError('BROWSER_ERROR',error);throw error}this.client=client;client.on('qr',async qr=>{if(this.client!==client||this.destroying)return;try{this.qr=qr;this.status='qr';this.lastEvent={type:'qr',at:nowIso()};await this.persistMeta({lastEvent:this.lastEvent});this.notification('تم توليد QR جديد','warning');await this.sendToExternalApi('/webhook/qr',{session:this.name,qrCode:qr,timestamp:nowIso()})}catch(error){await this.recordError('QR_HANDLER_ERROR',error)}});client.on('authenticated',async()=>{if(this.client!==client||this.destroying)return;this.status='authenticated';this.lastEvent={type:'authenticated',at:nowIso()};await this.persistMeta({lastEvent:this.lastEvent});this.notification('تمت المصادقة بنجاح','success')});client.on('ready',async()=>{if(this.client!==client||this.destroying)return;this.status='connected';this.qr=null;this.info=client?.info||null;this.lastError=null;this.lastEvent={type:'ready',at:nowIso()};await this.persistMeta({lastEvent:this.lastEvent});this.notification('الجلسة متصلة وجاهزة','success',{phone:this.info?.wid?.user||null});await this.sendToExternalApi('/webhook/session-status',{session:this.name,status:'connected',info:this.info,timestamp:nowIso()})});client.on('message',async message=>{if(this.client!==client||this.destroying)return;try{await this.handleIncomingMessage(message)}catch(error){await this.recordError('MESSAGE_HANDLER_ERROR',error)}});client.on('disconnected',async reason=>{if(this.client!==client)return;this.client=null;this.status='disconnected';this.info=null;this.qr=null;this.lastError=reason||'unknown';this.lastEvent={type:'disconnected',reason:this.lastError,at:nowIso()};await this.persistMeta({lastEvent:this.lastEvent,disconnectReason:this.lastError,qrAvailable:false});this.notification(`تم قطع الاتصال: ${this.lastError}`,'error');await this.sendToExternalApi('/webhook/session-status',{session:this.name,status:'disconnected',reason:this.lastError,timestamp:nowIso()})});client.on('auth_failure',async message=>{if(this.client!==client||this.destroying)return;this.status='error';this.lastError=message||'Authentication failure';this.lastEvent={type:'auth_failure',at:nowIso()};await this.persistMeta({lastEvent:this.lastEvent});await this.recordError('AUTH_FAILURE',new Error(this.lastError));this.notification(`فشل المصادقة: ${this.lastError}`,'error')});try{await client.initialize();return this.serialize()}catch(error){if(this.client===client)this.client=null;try{client.removeAllListeners()}catch(_){}this.status='error';this.lastError=error.message;this.lastEvent={type:'initialize_error',at:nowIso()};await this.persistMeta({lastEvent:this.lastEvent});await this.recordError('INITIALIZE_ERROR',error);throw error}}
  async stop(){if(this.stopPromise)return this.stopPromise;this.stopPromise=this._stopInternal().finally(()=>{this.stopPromise=null});return this.stopPromise}
  async _stopInternal(){this.destroying=true;const client=this.client;this.client=null;if(client){try{client.removeAllListeners();await client.destroy()}catch(error){await this.recordError('STOP_ERROR',error)}}this.status='disconnected';this.qr=null;this.info=null;this.lastEvent={type:'stop',at:nowIso()};await this.persistMeta({stopped:true,lastEvent:this.lastEvent,qrAvailable:false});this.notification('تم إيقاف الجلسة','warning');return this.serialize()}
  async logout(){if(!this.client)throw new Error('لا توجد جلسة تعمل');const client=this.client;try{await client.logout()}finally{if(this.client===client)this.client=null}this.status='disconnected';this.qr=null;this.info=null;this.lastEvent={type:'logout',at:nowIso()};await this.persistMeta({loggedOut:true,lastEvent:this.lastEvent,qrAvailable:false});this.notification('تم تسجيل الخروج من واتساب','warning');return this.serialize()}
  async restart(){await this.stop();return this.start()}
  async regenerateQr(){if(this.status==='connected')throw new Error('الجلسة متصلة. استخدم إعادة المصادقة لإنشاء QR جديد.');await this.stop();return this.start()}
  async reauthenticate(){await this.stop();await fs.remove(path.join(this.rootDir,'sessions',`session-${this.name}`)).catch(()=>{});this.status='idle';this.qr=null;this.info=null;this.lastError=null;this.lastEvent={type:'reauthenticate',at:nowIso()};await this.persistMeta({status:'idle',lastEvent:this.lastEvent});return this.start()}
  async sendMessage(phoneNumber,message,media=null){if(!this.client||this.status!=='connected')throw new Error('الجلسة غير جاهزة للإرسال');const jid=normalizePhoneToJid(phoneNumber);let content=message||'';if(media?.buffer)content=new MessageMedia(media.mimetype,media.buffer.toString('base64'),media.filename,media.size);const sent=media?.buffer?await this.client.sendMessage(jid,content,{caption:message||'',sendSeen:false}):await this.client.sendMessage(jid,content,{sendSeen:false});this.stats.outgoingCount+=1;this.stats.lastMessageAt=nowIso();this.lastEvent={type:'outgoing_message',at:nowIso()};await this.store.appendMessage(this.name,{session:this.name,direction:'out',messageId:sent?.id?._serialized||null,to:phoneNumber,body:message||'',type:media?'media':'text',timestamp:nowIso()});await this.persistMeta({stats:this.stats,lastEvent:this.lastEvent});await this.sendToExternalApi('/webhook/whatsapp',{session:this.name,direction:'out',messageId:sent?.id?._serialized||null,to:phoneNumber,body:message||'',type:media?'media':'text',timestamp:nowIso()});this.notification(`تم إرسال رسالة إلى ${phoneNumber}`,'info');return{success:true,messageId:sent?.id?._serialized||null}}
  async handleIncomingMessage(message){const data={session:this.name,direction:'in',messageId:message.id?._serialized||null,from:message.from||null,to:message.to||null,body:message.body||'',type:message.type||'unknown',hasMedia:!!message.hasMedia,mediaSkipped:!!message.hasMedia,timestamp:nowIso()};this.stats.incomingCount+=1;this.stats.lastMessageAt=nowIso();this.lastEvent={type:'incoming_message',at:nowIso()};await this.store.appendMessage(this.name,data);await this.persistMeta({stats:this.stats,lastEvent:this.lastEvent});await this.sendToExternalApi('/webhook/whatsapp',data);const preview=(data.body||'').slice(0,80)||(data.hasMedia?'رسالة تحتوي على ملف':'رسالة واردة');this.notification(`رسالة واردة: ${preview}`,'info',{from:data.from,hasMedia:data.hasMedia})}
  async sendToExternalApi(endpoint,payload){if(!this.apiBaseUrl)return;try{const body=JSON.stringify({...payload,botId:this.name});const signature=`sha256=${crypto.createHmac('sha256',this.webhookSecret).update(body).digest('hex')}`;await axios.post(`${this.apiBaseUrl}${endpoint}`,body,{timeout:15000,headers:{'content-type':'application/json','x-whatsapp-signature':signature,'x-whatsapp-event':endpoint.split('/').pop()},maxContentLength:1024*1024,maxBodyLength:1024*1024})}catch(error){await this.recordError('EXTERNAL_API_ERROR',error,{endpoint})}}
  async recordError(type,error,extra={}){await this.store.appendError(this.name,{type,message:error?.message||String(error),timestamp:nowIso(),...extra})}
  async getQrImageDataUrl(){return this.qr?QRCode.toDataURL(this.qr,{errorCorrectionLevel:'M',margin:2,scale:8}):null}
  async getPublicStatus(){const [messages,errors,notifications]=await Promise.all([this.store.getMessages(this.name,10,0),this.store.getErrors(this.name,10),this.store.getNotifications(this.name,15)]);return{...this.serialize(),messages,errors,notifications,qrTextAvailable:!!this.qr,qrDataUrl:this.qr?await this.getQrImageDataUrl():null}}
  getApiEndpoints(){const base=`/api/sessions/${encodeURIComponent(this.name)}`;return{status:`${base}/status`,qr:`${base}/qr`,qrImage:`${base}/qr-image`,connect:`${base}/connect`,disconnect:`${base}/disconnect`,restart:`${base}/restart`,reauthenticate:`${base}/reauthenticate`,logout:`${base}/logout`,delete:base,send:`${base}/send`,messages:`${base}/messages`,errors:`${base}/errors`,notifications:`${base}/notifications`}}
  async destroyCompletely(){await this.stop();await fs.remove(path.join(this.rootDir,'sessions',`session-${this.name}`)).catch(()=>{});await Promise.all([fs.remove(this.store.sessionFile(this.name)).catch(()=>{}),fs.remove(this.store.messageFile(this.name)).catch(()=>{}),fs.remove(this.store.errorFile(this.name)).catch(()=>{}),fs.remove(this.store.notificationFile(this.name)).catch(()=>{})])}
}
class SessionManager{
 constructor({rootDir,store,chromePath=null,io=null}){this.rootDir=rootDir;this.store=store;this.chromePath=chromePath||null;this.io=io;this.sessions=new Map()}
 async ensureSession(name,apiBaseUrl=null,backupPhone=null){let session=this.sessions.get(name);if(!session){session=new WhatsAppSession({name,rootDir:this.rootDir,store:this.store,apiBaseUrl,backupPhone,chromePath:this.chromePath,io:this.io});await session.loadMeta();this.sessions.set(name,session)}if(apiBaseUrl!==null&&apiBaseUrl!==undefined&&apiBaseUrl!==''){session.apiBaseUrl=validateApiBaseUrl(apiBaseUrl);await session.persistMeta({apiBaseUrl:session.apiBaseUrl})}if(backupPhone){session.backupPhone=backupPhone;await session.persistMeta({backupPhone})}return session}
 async createSession(name,options={}){if(await this.store.getSessionMeta(name)||this.sessions.has(name))throw new Error('هذه الجلسة موجودة بالفعل');const session=new WhatsAppSession({name,rootDir:this.rootDir,store:this.store,apiBaseUrl:options.apiBaseUrl||null,backupPhone:options.backupPhone||null,description:options.description||'',webhookSecret:options.webhookSecret||null,chromePath:this.chromePath,io:this.io});await session.persistMeta({createdAt:nowIso(),clientUsername:options.clientUsername||null,autoStart:options.autoStart===true,status:'idle'});this.sessions.set(name,session);return session}
 async removeSession(name){const session=this.sessions.get(name)||await this.ensureSession(name);try{await session.destroyCompletely()}finally{this.sessions.delete(name)}}
 async listStatuses(options={}){const names=await this.store.listSessionNames();const states=[];for(const name of names){const session=await this.ensureSession(name);const status=await session.getPublicStatus();if(options.includeSecrets){status.webhookSecret=session.webhookSecret}states.push(status)}return states}
}
module.exports={SessionManager,WhatsAppSession};